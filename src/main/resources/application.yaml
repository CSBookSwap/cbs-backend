spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:postgresql://${BOOKSWAP_POSTGRES_HOST:localhost}:${BOOKSWAP_POSTGRES_PORT:5432}/bookswap_db
    username: bookswap-api
    password: ${STORE_POSTGRES_PASSWORD:bookswapPassWord}
    driver-class-name: org.postgresql.Driver

server:
  port: 8082

